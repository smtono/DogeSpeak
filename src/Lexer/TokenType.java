package Lexer;

// TOKEN TYPES
public enum TokenType {
    IDENTIFIER,
    INTEGER,
    FLOAT,
    LEFT_PARENTHESIS,
    RIGHT_PARENTHESIS,
    EQUAL,
    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE,
    UNEXPECTED,
    NONE
}